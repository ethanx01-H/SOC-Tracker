services:
  soc-alert-tracker:
    build: .
    image: soc-alert-tracker:local
    env_file: .env
    environment:
      SQLITE_PATH: /app/data/fastapi.sqlite3
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
